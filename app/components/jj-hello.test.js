module.exports = function(ngModule) {
  describe('jj-hello', function() {
    beforeEach(window.module(ngModule.name));

    it('should test properly', function() {
      assert.isTrue(false);
    });
  });
};
